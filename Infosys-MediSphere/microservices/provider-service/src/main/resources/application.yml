server:
  port: 8086

spring:
  application:
    name: provider-service
  data:
    mongodb:
      uri: mongodb://localhost:27017/medisphere_provider

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
