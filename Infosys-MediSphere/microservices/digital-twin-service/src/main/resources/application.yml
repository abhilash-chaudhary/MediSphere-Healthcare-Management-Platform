server:
  port: 8084

spring:
  application:
    name: digital-twin-service
  data:
    mongodb:
      uri: mongodb://localhost:27017/medisphere_twin

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
